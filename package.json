{
  "name": "ldr-lyrics-bot",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "generate-types": "wrangler types",
    "type-check": "generate-types && tsc"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "wrangler": "^4.94.0"
  },
  "dependencies": {
    "@atproto/api": "^0.13.15"
  }
}
